# Artemis-only features (not in Founderszoo)

- [ ] Statused blocks (`planned/in_progress/complete/bad`) with status cycling via `Enter`
- [ ] Visual selection mode (`v`) with range selection overlay and range shift/delete
- [ ] Undo stack for edits, delete, shift (`u`)
- [ ] Row overlay when both halves share the same title
- [ ] Status propagation across adjacent blocks with the same title
- [ ] Shift-backwards guard that blocks moving before current time
